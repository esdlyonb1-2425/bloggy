{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    <button type="submit" class="btn btn-success">Post this</button>
    {{ form_end(form) }}


{% endblock %}

{% block title %}

{% endblock %}

